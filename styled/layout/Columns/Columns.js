import styled from "styled-components";
import columnsStyles from "./columns.module.scss";

console.log("columnsStyles", columnsStyles);

const Columns = styled.div`
  ${columnsStyles}
`;

export default Columns;
